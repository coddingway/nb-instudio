/** @type {import('tailwindcss').Config} */
module.exports = {
    // logic: Look in the root folder (./) and any subfolders for .html or .js files
    content: ["./**/*.{html,js}"], 
    theme: {
      extend: {},
    },
    plugins: [],
  }